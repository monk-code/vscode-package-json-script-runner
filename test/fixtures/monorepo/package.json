{
  "name": "test-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "build": "echo 'Building all packages'",
    "test": "echo 'Testing all packages'",
    "lint": "echo 'Linting all packages'"
  }
}
